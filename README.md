# 🛒 MarketPlace - Десктопное приложение маркетплейса

![Qt](https://img.shields.io/badge/Qt-6.10+-green.svg)
![C++](https://img.shields.io/badge/C++-17-blue.svg)
![Platform](https://img.shields.io/badge/Platform-Windows%20%7C%20Linux%20%7C%20macOS-lightgrey.svg)

Десктопное приложение интернет-магазина, разработанное на **Qt 6** и **C++17**. Приложение имитирует функционал маркетплейса в стиле Wildberries с поддержкой тем оформления, корзиной покупок и админ-панелью.

---

## 📋 Содержание

- [Возможности](#-возможности)
- [Скриншоты](#-скриншоты)
- [Структура проекта](#-структура-проекта)
- [Требования](#-требования)
- [Установка и сборка](#-установка-и-сборка)
- [Архитектура](#-архитектура)
- [Описание модулей](#-описание-модулей)
- [Темы оформления](#-темы-оформления)
- [База данных](#-база-данных)
- [Для разработчиков](#-для-разработчиков)

---

## ✨ Возможности

### Для покупателей
- 🔐 Регистрация и авторизация пользователей
- 📦 Просмотр каталога товаров с категориями
- 🔍 Поиск товаров
- 🛒 Корзина покупок с изменением количества
- 👤 Личный профиль с балансом
- 🎨 Выбор темы оформления (светлая/тёмная/Wildberries)

### Для администраторов
- ➕ Добавление новых товаров
- ✏️ Редактирование существующих товаров
- 🗑️ Удаление товаров
- 📊 Управление складскими остатками

---

## 📸 Скриншоты

> Скриншоты можно добавить в папку `docs/screenshots/`

---

## 📁 Структура проекта

```
wild/
├── 📁 include/              # Заголовочные файлы (.h)
│   ├── 📁 core/             # Ядро приложения
│   │   ├── database.h       # Класс работы с данными
│   │   └── theme_manager.h  # Менеджер тем (синглтон)
│   ├── 📁 models/           # Модели данных
│   │   ├── user.h           # Модель пользователя
│   │   ├── product.h        # Модель товара
│   │   └── cart.h           # Модель корзины
│   └── 📁 ui/               # Пользовательский интерфейс
│       ├── mainwindow.h     # Главное окно
│       ├── loginwindow.h    # Окно входа
│       ├── registerwindow.h # Окно регистрации
│       ├── adminpanel.h     # Админ-панель
│       ├── cartwidget.h     # Виджет корзины
│       ├── profilewidget.h  # Виджет профиля
│       └── productcard.h    # Карточка товара
│
├── 📁 src/                  # Исходные файлы (.cpp)
│   ├── main.cpp             # Точка входа
│   ├── 📁 core/             # Реализация ядра
│   ├── 📁 models/           # Реализация моделей
│   └── 📁 ui/               # Реализация UI
│
├── 📁 forms/                # Qt Designer формы (.ui)
│   └── mainwindow.ui
│
├── 📁 resources/            # Ресурсы приложения
│   └── 📁 styles/           # QSS стили
│       ├── style_light.qss  # Светлая тема
│       ├── style_dark.qss   # Тёмная тема
│       ├── style_wb.qss     # Тема Wildberries
│       └── styles.qrc       # Файл ресурсов Qt
│
├── 📁 data/                 # Данные приложения (создаётся при запуске)
│   ├── users.dat            # Файл пользователей
│   └── products.dat         # Файл товаров
│
├── 📁 docs/                 # Документация
│   └── 📁 screenshots/      # Скриншоты
│
├── 📁 build/                # Директория сборки (не коммитить!)
│
├── wild.pro                 # Файл проекта Qt
└── README.md                # Этот файл
```

---

## 📋 Требования

| Компонент | Минимальная версия |
|-----------|-------------------|
| Qt        | 6.0+              |
| Компилятор C++ | C++17 (GCC 8+ / MSVC 2019+ / Clang 7+) |
| CMake (опционально) | 3.16+ |

### Зависимости Qt
- `Qt Core`
- `Qt Gui`
- `Qt Widgets`

---

## 🚀 Установка и сборка

### Способ 1: Qt Creator (рекомендуется)

1. Откройте Qt Creator
2. **Файл** → **Открыть файл или проект**
3. Выберите `wild.pro`
4. Настройте комплект сборки (Kit) с Qt 6
5. Нажмите **Ctrl+R** для сборки и запуска

### Способ 2: Командная строка (qmake)

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd wild

# Создайте директорию сборки
mkdir build && cd build

# Сгенерируйте Makefile
qmake ../wild.pro

# Соберите проект
make -j$(nproc)   # Linux/macOS
# или
mingw32-make      # Windows с MinGW
# или
nmake             # Windows с MSVC

# Запустите
./wild            # Linux/macOS
wild.exe          # Windows
```

### Способ 3: Командная строка (CMake) — если добавите CMakeLists.txt

```bash
mkdir build && cd build
cmake ..
cmake --build . --config Release
```

---

## 🏗️ Архитектура

Проект следует архитектуре **MVC** (Model-View-Controller) с разделением на слои:

```
┌─────────────────────────────────────────────────────────┐
│                      UI Layer                           │
│  (MainWindow, LoginWindow, ProductCard, CartWidget...)  │
├─────────────────────────────────────────────────────────┤
│                    Core Layer                           │
│          (Database, ThemeManager)                       │
├─────────────────────────────────────────────────────────┤
│                   Models Layer                          │
│            (User, Product, Cart)                        │
├─────────────────────────────────────────────────────────┤
│                   Data Storage                          │
│              (data/*.dat files)                         │
└─────────────────────────────────────────────────────────┘
```

---

## 📦 Описание модулей

### 🔧 Core (Ядро)

#### `Database`
Класс для работы с данными пользователей и товаров.

```cpp
Database db("data/");

// Аутентификация
User* user = db.authenticateUser("username", "password");

// Регистрация (с админ-кодом для администратора)
db.registerUser("user", "pass", "email@test.com", "ADMIN_CODE");

// Работа с товарами
auto products = db.getAllProducts();
auto electronics = db.getProductsByCategory("Электроника");
auto found = db.searchProducts("смартфон");
```

#### `ThemeManager`
Синглтон для управления темами приложения.

```cpp
// Получение экземпляра
ThemeManager& tm = ThemeManager::instance();

// Применение темы
tm.applyTheme(ThemeType::Dark);

// Получение текущей темы
ThemeType current = tm.currentTheme();

// Подписка на изменения
connect(&tm, &ThemeManager::themeChanged, this, &MyClass::onThemeChanged);
```

### 📊 Models (Модели данных)

#### `User`
Модель пользователя с ролями (Customer/Admin).

| Поле | Тип | Описание |
|------|-----|----------|
| `m_id` | `int` | Уникальный идентификатор |
| `m_username` | `string` | Имя пользователя |
| `m_password` | `string` | Пароль |
| `m_email` | `string` | Email |
| `m_phone` | `string` | Телефон |
| `m_address` | `string` | Адрес доставки |
| `m_balance` | `double` | Баланс счёта |
| `m_role` | `UserRole` | Роль (Customer/Admin) |

#### `Product`
Модель товара.

| Поле | Тип | Описание |
|------|-----|----------|
| `m_id` | `int` | Уникальный идентификатор |
| `m_name` | `string` | Название товара |
| `m_description` | `string` | Описание |
| `m_category` | `string` | Категория |
| `m_brand` | `string` | Бренд |
| `m_price` | `double` | Цена |
| `m_oldPrice` | `double` | Старая цена (для скидок) |
| `m_discount` | `int` | Процент скидки |
| `m_stock` | `int` | Остаток на складе |
| `m_rating` | `double` | Рейтинг (0-5) |

#### `Cart`
Корзина покупок с методами для управления товарами.

```cpp
Cart cart(userId);
cart.addItem(product, 2);        // Добавить 2 шт
cart.updateQuantity(productId, 5); // Изменить количество
cart.removeItem(productId);       // Удалить
double total = cart.getTotalPrice();
```

### 🖼️ UI (Пользовательский интерфейс)

| Виджет | Описание |
|--------|----------|
| `MainWindow` | Главное окно с навигацией и каталогом |
| `LoginWindow` | Форма входа в систему |
| `RegisterWindow` | Форма регистрации |
| `AdminPanel` | Панель администратора для управления товарами |
| `CartWidget` | Виджет корзины с итогами |
| `ProfileWidget` | Профиль пользователя |
| `ProductCard` | Карточка товара в каталоге |

---

## 🎨 Темы оформления

Приложение поддерживает 3 темы:

| Тема | Файл | Описание |
|------|------|----------|
| 🌞 Светлая | `style_light.qss` | Классическая светлая тема |
| 🌙 Тёмная | `style_dark.qss` | Тёмная тема для комфортной работы |
| 💜 Wildberries | `style_wb.qss` | Фирменный стиль в цветах WB |

Тема сохраняется в `QSettings` и загружается при следующем запуске.

### Добавление новой темы

1. Создайте файл `resources/styles/style_mytheme.qss`
2. Добавьте его в `styles.qrc`
3. Добавьте новое значение в `enum class ThemeType`
4. Обновите методы в `ThemeManager`

---

## 💾 База данных

Данные хранятся в текстовых файлах в папке `data/`:

- `users.dat` — сериализованные данные пользователей
- `products.dat` — сериализованные данные товаров

При первом запуске создаются демо-товары.

### Секретный код администратора

Для регистрации с правами администратора используйте секретный код при регистрации. Код задан в `database.cpp` в константе `ADMIN_SECRET_CODE`.

---

## 👨‍💻 Для разработчиков

### Стиль кода

- Именование переменных-членов класса: `m_variableName`
- Использование `nullptr` вместо `NULL`
- Комментарии на русском языке
- Документация классов в заголовочных файлах

### Полезные команды

```bash
# Очистка сборки
make clean

# Пересборка
make clean && make -j$(nproc)

# Запуск с отладочным выводом Qt
QT_DEBUG_PLUGINS=1 ./wild
```

### Добавление нового виджета

1. Создайте заголовочный файл в `include/ui/`
2. Создайте файл реализации в `src/ui/`
3. Добавьте пути в `wild.pro`
4. Если нужна форма — создайте `.ui` в `forms/`

### Git workflow

```bash
# Не коммитьте:
# - build/
# - *.pro.user
# - data/*.dat (пользовательские данные)
```

---

## 📝 TODO / Планы развития

- [ ] Добавить изображения товаров
- [ ] Реализовать историю заказов
- [ ] Добавить систему отзывов
- [ ] Подключить SQLite вместо файловой БД
- [ ] Добавить unit-тесты
- [ ] Реализовать фильтры по цене и рейтингу

---

## 🤝 Участие в разработке

1. Сделайте Fork репозитория
2. Создайте ветку для фичи (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

---

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Подробности в файле `LICENSE`.

---

## 📞 Контакты

Если у вас есть вопросы по проекту, создайте Issue в репозитории.

---

<p align="center">
  Сделано с ❤️ и Qt
</p>